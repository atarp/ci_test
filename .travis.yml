dist: trusty

language: cpp 
compiler: gcc

matrix:
  include:
    # works on Precise and Trusty
    - os: linux   
addons: 
    apt: 
    sources: 
    - ubuntu-toolchain-r-test 
script:
    - cmake .
    - cmake --build . 
 

